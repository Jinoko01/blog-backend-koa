let Koa,Router,bodyParser,mongoose,api;_e82‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_e82‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_e82‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_e82‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_e82‍.w("./api",[["default",["api"],function(v){api=v}]]);require("dotenv").config();







const { PORT, MONGO_URL } = process.env;

mongoose
  .connect(MONGO_URL)
  .then(() => {
    console.log("Connected to MongoDB");
  })
  .catch((e) => {
    _e82‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

router.use("/api", api.routes());

app.use(bodyParser());

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  console.log(`Listening to port ${port}`);
});
